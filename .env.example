# Optional overrides — sensible defaults are baked into docker-compose.yml.

# PromptPay payee shown on generated QR codes (fake demo number).
TEMPLE_PROMPTPAY_ID=0899999999

# Engine log level
RUST_LOG=sila_engine=info,tower_http=info
